-- Bukowski Foundation Bridge
-- Phase 3 will introduce the cloud sync tables and policies that mirror
-- the local-first operational model defined in packages/db and docs/foundation/schema-v1.md.
-- This placeholder exists to lock directory ownership and migration intent
-- without prematurely shipping a half-defined remote schema.

select 1;
